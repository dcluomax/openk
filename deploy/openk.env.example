# openk 服务端环境变量示例
#
# 用法（三种都直接吃这个格式，不需要额外依赖）：
#   docker run --env-file openk.env ...
#   systemd 单元里写 EnvironmentFile=/etc/openk/openk.env
#   本机调试：set -a; . ./openk.env; set +a
#
# 复制一份再改，别把改过的版本提交上去：
#   cp deploy/openk.env.example openk.env

# ── 存储路径 ────────────────────────────────────────────────
# 数据总目录。下面几项不设的话，都在它下面。
OPENK_DATA_DIR=/data

# 任务目录：每首歌的音频、分离结果、歌词、录音都在这儿，占空间的大头。
# 想把它单独放到 NAS 或大容量盘上，就在这里指过去。
#OPENK_JOBS_DIR=/mnt/nas/openk/jobs

# 模型缓存。留空＝各库自己的默认位置，而 audio-separator 的默认位置是 /tmp，
# 有些系统重启就清空，于是每次都要重新下几百 MB。模型总量可达数 GB，
# 建议明确指到一块持久、够快的盘上（分离和对齐都要反复读它）。
#OPENK_MODELS_DIR=/var/lib/openk/models

# HTTPS 证书目录（scripts/make_cert.py 的输出位置）。
#OPENK_CERTS_DIR=/data/certs

# ── 网络 ───────────────────────────────────────────────────
# 只给本机用就保持 127.0.0.1；要让手机/平板连进来才改 0.0.0.0。
OPENK_HOST=0.0.0.0
OPENK_PORT=8000

# 麦克风必须在「安全上下文」下才能用，局域网访问就得上 HTTPS。
# 两个都设上才会启用。前面已经有反向代理终结 TLS 的话，这两项留空。
#OPENK_SSL_CERTFILE=/data/certs/openk.crt
#OPENK_SSL_KEYFILE=/data/certs/openk.key

# ── 处理 ───────────────────────────────────────────────────
#OPENK_WHISPER_MODEL=small
#OPENK_WHISPER_LANGUAGE=
#OPENK_MAX_WORKERS=1
#OPENK_MAX_SONG_SECONDS=420

# YouTube 限流或要求登录时，用 cookies 绕过。
#OPENK_COOKIEFILE=/data/cookies.txt

# ── 远程算力（可选）──────────────────────────────────────────
# 交给 worker 跑的步骤。留空＝全部本地执行，行为与单机版完全一致。
# 设成下面这行之后，本机就不需要 torch / onnxruntime 了。
#OPENK_REMOTE_STEPS=separate,transcribe,align
# 与 worker 共用的口令，两端必须一致。请自行生成，不要沿用示例值。
#OPENK_WORKER_TOKEN=换成你自己的随机口令
# worker 离线时任务排队等待而不是失败；0 ＝ 无限等待。
#OPENK_REMOTE_WAIT_TIMEOUT=0
