# openk 运行时（轻量）依赖 —— Web 服务与下载
# 安装：pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# 繁体↔简体转换：中文歌搜歌词库时提高命中率（LRCLIB 多存简体，标题常繁体）。纯 Python、无编译。
zhconv>=1.4

# yt-dlp：YouTube 反爬变化很快，强烈建议用 nightly 版本，并安装 Deno (JS runtime)：
#   pip install -U --pre "yt-dlp[default]"     # nightly（含 JS 引擎支持）
#   brew install deno                           # macOS 的 JS runtime
# 无 JS runtime 时 YouTube 提取已被官方标为 deprecated，并可能导致 403。详见 README。
yt-dlp[default]>=2025.1.1

# 只有 scripts/make_cert.py 生成自签 HTTPS 证书时才用得到。
# 不需要局域网麦克风、或已有反向代理终结 TLS 的话，可以不装。
cryptography>=42.0
