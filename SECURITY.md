# 安全部署与漏洞报告

## 支持的边界

OpenK 面向可信局域网或已有身份认证反向代理后的部署，**不提供全站账号登录**。
六位配对码、电视租约和控制凭据只保护共享房间，不能保护曲库、下载、录音或管理 API。
不要把未认证的 HTTP 端口直接映射到公网。

- 跨机器使用 HTTPS 或经过认证的加密隧道；浏览器侧 TLS 不会自动保护 worker 的独立连接。
- 浏览器默认同源访问；修改 API 的请求会在副作用发生前拒绝不匹配或 `null` 来源。
  必须跨域时通过 `OPENK_ALLOWED_ORIGINS` 明确列出可信来源，不接受通配符。无 Origin 的
  worker／CLI 请求仍按原来的网络和口令边界处理，来源检查不是客户端身份认证。
- 代理需保留完整 Host（包括非标准端口），并仅信任正确的转发来源；也可在上述白名单中填写
  页面实际的 HTTPS 来源。代理自身仍应提供认证、CSRF 防护和 Host 限制。
- worker 使用随机口令；共享存储账户、worker 主机和模型来源都必须可信。持有共享存储写权限
  的 worker 不是沙箱中的不可信租户。
- 容器按适当的非 root UID/GID 运行，已有媒体库只读挂载；数据、归档和模型缓存分开授权。
- 单个数据目录只由一个 API 进程管理。增加推理 worker 不等于可以启动多个共享状态的 API 副本。
- `/media` 拒绝 SVG、HTML、脚本等非媒体文档，并对允许的内容设置固定 MIME、
  `nosniff` 和沙箱 CSP，防止下载文件以应用同源文档的权限执行。
- 录音上传上限为 100 MiB；反向代理应设置相同上限。配对、房间、队列和命令历史也有容量限制，
  但这些限制不是面向恶意公网流量的完整防护。

## 凭据与公开素材

实际 `.env`、`worker.env`、`openk.env`、口令文件、私钥、证书、任务数据和 worker 临时目录
均不应提交或进入镜像构建上下文。示例使用文档保留地址与占位值；自行填写的部署信息留在本机。
不要把口令放在命令行参数、截图、日志或公开 issue 中。若真实凭据曾经公开，删除文件不能撤销
已复制的凭据，应先轮换；历史清理需要单独处理。

文档截图来自隔离环境的虚构曲目、原创示例歌词、合成音轨及程序生成封面，不访问真实麦克风
或曲库。第三方许可证、作者署名和项目 GitHub/GHCR 地址不是脱敏对象，必须保留。

## 升级与报告

升级前备份数据、镜像摘要和 worker 代码，等待任务空闲；控制端与 worker 协议必须同步升级。
见 [变更与升级说明](CHANGELOG.md)。自动化回归和安全审查不等于没有漏洞，也不代替 Fire TV、
实体麦克风、操作系统权限或实际网络边界的验证。

发现问题时，如仓库已启用私密安全报告，请优先使用该入口；否则先发布不含敏感细节的联系请求，
与维护者确认私密沟通方式。不要在公开报告中上传实际口令、有效房间凭据、私有服务器地址或
未经授权的媒体；使用最小化的合成复现材料。
